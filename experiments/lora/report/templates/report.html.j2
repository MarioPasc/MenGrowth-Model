<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>{{ title }}</title>
<style>
{{ css }}
</style>
</head>
<body>

<header>
  <h1>{{ title }}</h1>
  <p class="subtitle">{{ subtitle }}</p>
  <p class="meta">Generated: {{ generated_date }} | Mode: {{ mode }} | Experiments: {{ num_experiments }}</p>
</header>

<nav>
  <h2>Table of Contents</h2>
  <ol>
  {% for section in sections %}
    <li><a href="#{{ section.section_id }}">{{ section.title }}</a></li>
  {% endfor %}
  </ol>
</nav>

<main>
{% for section in sections %}
<section id="{{ section.section_id }}">
  <h2>{{ section.title }}</h2>

  {% for paragraph in section.paragraphs %}
  <p>{{ paragraph }}</p>
  {% endfor %}

  {% for fig_name in section.figure_names %}
    {% if fig_name in figures %}
  <figure>
    <img src="data:image/png;base64,{{ figures[fig_name].png_base64 }}" alt="{{ figures[fig_name].caption }}" loading="lazy">
    <figcaption><strong>Figure:</strong> {{ figures[fig_name].caption }}</figcaption>
  </figure>
    {% endif %}
  {% endfor %}

  {% for tbl_name in section.table_names %}
    {% if tbl_name in tables %}
  <div class="table-container">
    {{ tables[tbl_name] | safe }}
  </div>
    {% endif %}
  {% endfor %}
</section>
{% endfor %}
</main>

<footer>
  <p>Generated by LoRA Ablation Report Generator | MenGrowth-Model</p>
  <p>{{ generated_date }}</p>
</footer>

</body>
</html>
