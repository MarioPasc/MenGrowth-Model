# DEPRECATED (2026-04-22) — kept only for backward compatibility.
#
# The encoder-only Picasso overrides (batch_size=2, grad_accum_steps=2,
# save_per_member_probs_all=true, encoder-only output_dir) are now baked
# into experiments/uncertainty_segmentation/config/picasso/config_picasso.yaml
# because encoder-only is the default regime.
#
# The slurm launcher no longer applies this file. See
# experiments/uncertainty_segmentation/config/encoder_only.yaml for details.
#
# This file is intentionally empty (no keys) so that any stray OmegaConf
# merge referencing it is a no-op.
